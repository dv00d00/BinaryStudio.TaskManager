@if(Request.IsAuthenticated) {
    <text>
        Добро пожаловать,  
        <strong>
            @User.Identity.Name
        </strong>!
        [ @Html.ActionLink("Выйти", "LogOff", "Account") ]
    </text>  
}
else {
    <br/>@:[ @Html.ActionLink("Войти", "LogOn", "Account") ]
    <br/>@:[ @Html.ActionLink("Login with LinkedOn", "LogonStart", "LinkedIn") ]
}
